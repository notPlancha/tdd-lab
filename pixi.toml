[project]
channels = ["conda-forge"]
name = "tdd-lab"
platforms = ["win-64"]

[tasks]
format = "ruff format"
lint = "ruff check"
task2 = "python ./TDD_lab_student/Task2_unittest.py"
coverage = "coverage run -m unittest ./TDD_lab_student/*; coverage report"

[tasks.report]
cmd = "coverage report"
depends-on = ["coverage"] # this isnt running if this isnt sucessful, change that

[dependencies]
python = ">=3.13.2,<3.14"
coverage = ">=7.6.12,<8"
ruff = ">=0.9.6,<0.10"
